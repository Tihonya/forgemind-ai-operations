{
  "gates": {
    "scope": {
      "enabled": true,
      "required": true,
      "description": "Check that changes are within allowed paths and don't touch forbidden files"
    },
    "json_syntax": {
      "enabled": true,
      "required": true,
      "description": "Validate JSON syntax in modified/created JSON files"
    },
    "yaml_syntax": {
      "enabled": true,
      "required": true,
      "description": "Validate YAML syntax in modified/created YAML files"
    },
    "targeted_tests": {
      "enabled": true,
      "required": true,
      "assertion_gate": true,
      "description": "Run targeted tests from story manifest or diff-based selection"
    },
    "lint": {
      "enabled": true,
      "required": true,
      "scope_to_diff": true,
      "description": "Run ruff and mypy linters"
    },
    "secrets": {
      "enabled": true,
      "required": true,
      "scope_to_diff": true,
      "description": "Scan for accidentally committed secrets using inline regex patterns"
    },
    "git_diff_check": {
      "enabled": true,
      "required": false,
      "description": "Run git diff --check for whitespace errors"
    }
  },
  "defaults": {
    "max_repair_iterations": 3,
    "auto_commit": false,
    "dry_run": false
  }
}
