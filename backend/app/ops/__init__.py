"""Production operations tooling package (WP-P7-02).

Repository-owned, fail-closed helpers for Release 1 deployment:
- :mod:`app.ops.validate_config` — production configuration validation.
- :mod:`app.ops.embedding_smoke` — OpenRouter embedding smoke harness
  (offline preparation only; live execution requires separate
  authorization).
"""
